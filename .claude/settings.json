{
  "model": "claude-sonnet-4-6",
  "theme": "auto",
  "permissions": {
    "allow": [
      "Bash(mvn:*)",
      "Bash(npm:*)",
      "Bash(git:*)",
      "Bash(docker:*)",
      "Bash(docker-compose:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.env.CLAUDE_TOOL_INPUT_FILE_PATH||''; const bad=['db/migration/oracle','db/migration/postgresql/V1__','db/migration/postgresql/V2__']; const hit=bad.find(b=>p.includes(b)); if(hit){ console.error('GUARDRAIL: Jangan edit committed migration file: '+p+'. Buat versi baru (V3+) sebagai gantinya.'); process.exit(1); }\""
          }
        ]
      }
    ]
  }
}
